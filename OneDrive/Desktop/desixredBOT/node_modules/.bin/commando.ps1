#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/coffee$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/coffee$exe"  "$basedir/../commando/bin/cli.coffee" $args
  } else {
    & "$basedir/coffee$exe"  "$basedir/../commando/bin/cli.coffee" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "coffee$exe"  "$basedir/../commando/bin/cli.coffee" $args
  } else {
    & "coffee$exe"  "$basedir/../commando/bin/cli.coffee" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
