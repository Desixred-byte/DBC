import { Client, EmojiResolvable, MessageReaction, Snowflake, User } from "discord.js";
export interface ReactionRoleConfiguration {
    messageId: Snowflake;
    roleId: Snowflake;
    reaction: EmojiResolvable;
}
export default class ReactionRole {
    private client;
    private reverseConfig;
    constructor(client: Client, config: ReactionRoleConfiguration[]);
    private extractRole;
    private addReaction;
    removeReaction(reaction: MessageReaction, user: User): Promise<void>;
    teardown(): void;
}
