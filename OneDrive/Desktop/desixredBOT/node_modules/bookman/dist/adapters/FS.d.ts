import { BaseAdapter } from "./BaseAdapter";
import { LooseObject } from "../types";
export declare class FS extends BaseAdapter {
    set(value: string): LooseObject;
    get(): LooseObject;
    destroy(): boolean;
    init(): void;
}
