#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  exec "$basedir/coffee"  "$basedir/../commando/bin/cli.coffee" "$@"
else 
  exec coffee  "$basedir/../commando/bin/cli.coffee" "$@"
fi
